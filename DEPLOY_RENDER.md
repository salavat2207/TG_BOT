# Пошаговая инструкция по развертыванию на Render

## Шаг 1: Создание PostgreSQL базы данных на Render

### 1.1. Создание новой БД
1. Войдите в [dashboard.render.com](https://dashboard.render.com)
2. Нажмите **New +** в верхнем правом углу
3. Выберите **PostgreSQL**
4. Заполните форму:
   - **Name**: `tg-bot-db` (или любое другое имя)
   - **Database**: `video_analytics` (или оставьте по умолчанию)
   - **User**: будет создан автоматически
   - **Region**: выберите ближайший регион
   - **PostgreSQL Version**: выберите 15 или выше
   - **Plan**: выберите подходящий тариф (Free tier доступен)
5. Нажмите **Create Database**

### 1.2. Получение DATABASE_URL
1. После создания БД откройте её в списке сервисов
2. Перейдите на вкладку **Info**
3. Найдите раздел **Internal Database URL** или **Connection String**
4. Скопируйте строку подключения (формат: `postgresql://user:password@host:port/database`)
<!-- 5. **Сохраните эту строку** - это будет значение переменной `DATABASE_URL` -->

**⚠️ Важно:** Не публикуйте строку подключения в репозитории! Она содержит пароль.

**Важно:** Не публикуйте строку подключения в репозитории! Она содержит пароль.

**Пример:**
```
postgresql://user:password@dpg-xxxxx-a.oregon-postgres.render.com/video_analytics
```

---

## Шаг 2: Создание/настройка сервиса бота

### 2.1. Создание Background Worker
1. В dashboard.render.com нажмите **New +**
2. Выберите **Background Worker**
3. Подключите репозиторий:
   - Если репозиторий уже подключен: выберите его из списка
   - Если нет: нажмите **Connect account** и подключите GitHub
   - Выберите репозиторий `TG_BOT`
4. Заполните настройки:
   - **Name**: `tg-bot` (или любое другое)
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым (или `/` если требуется)
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`

### 2.2. Настройка переменных окружения
1. В настройках сервиса перейдите на вкладку **Environment**
2. Нажмите **Add Environment Variable** для каждой переменной:

| Имя переменной | Значение | Где взять |
|---------------|----------|-----------|
| `DATABASE_URL` | Строка подключения из шага 1.2 | Из настроек PostgreSQL |
| `TELEGRAM_BOT_TOKEN` | Токен бота | От @BotFather в Telegram |
| `GROQ_API_KEY` | API ключ | С https://console.groq.com/ |

**Важно:** Используйте **Internal Database URL** из PostgreSQL сервиса, а не External URL!

3. После добавления всех переменных нажмите **Save Changes**

---

## Шаг 3: Инициализация базы данных

### ⚠️ Важно: Таблицы создаются автоматически!

При первом подключении к БД таблицы создаются автоматически. Вам нужно только загрузить данные.

### 3.1. Загрузка данных (для бесплатной версии - Web Service)

**Таблицы уже созданы автоматически!** Теперь нужно загрузить данные из `videos.json`.

#### Вариант 1: Через браузер (самый простой)
1. После деплоя откройте ваш сервис в браузере
2. Добавьте к URL `/load-data`:
   ```
   https://your-service-name.onrender.com/load-data
   ```
3. Дождитесь загрузки (может занять 1-2 минуты)
4. Вы увидите сообщение: `{"status": "success", "message": "Данные загружены успешно. Бот готов к работе!"}`

#### Вариант 2: Через curl (командная строка)
```bash
curl https://your-service-name.onrender.com/load-data
```

#### Вариант 3: Через Shell (только для платных планов)
Если у вас есть доступ к Shell:
1. В dashboard откройте ваш сервис
2. Перейдите на вкладку **Shell**
3. Выполните: `python setup_db.py`

### 3.2. Проверка результата

После загрузки данных:
- Откройте бота в Telegram
- Отправьте команду `/start`
- Отправьте тестовый вопрос: "Сколько всего видео есть в системе?"
- Бот должен вернуть число (например: `1234`)

Если видите ошибки:
- **"DATABASE_URL не установлен"** - проверьте переменные окружения в настройках сервиса
- **"Connect call failed"** - убедитесь, что используете Internal Database URL, а не External
- **"Файл videos.json не найден"** - проверьте, что файл есть в репозитории
- **"relation videos does not exist"** - подождите несколько секунд, таблицы создаются автоматически при первом подключении

---

## Шаг 4: Запуск и проверка бота

### 4.1. Запуск сервиса
1. Убедитесь, что сервис запущен (статус должен быть **Live**)
2. Если сервис не запущен, нажмите **Manual Deploy** → **Deploy latest commit**

### 4.2. Проверка логов
1. Перейдите на вкладку **Logs**
2. Должны увидеть:
   ```
   Подключение к базе данных установлено
   ```
3. Если видите ошибки подключения к БД - проверьте `DATABASE_URL`

### 4.3. Тестирование в Telegram
1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением

### 4.4. Тестовый запрос
Отправьте боту вопрос:
```
Сколько всего видео есть в системе?
```

Бот должен вернуть число (например: `1234`)

---

## Решение проблем

### Проблема: "Connect call failed ('127.0.0.1', 5432)"
**Причина:** Используется External Database URL вместо Internal

**Решение:**
1. Откройте PostgreSQL сервис на Render
2. Перейдите на вкладку **Info**
3. Скопируйте **Internal Database URL** (не External!)
4. Обновите переменную `DATABASE_URL` в настройках Background Worker
5. Перезапустите сервис

### Проблема: "DATABASE_URL не установлен"
**Решение:**
1. Проверьте, что переменная `DATABASE_URL` добавлена в Environment Variables
2. Убедитесь, что значение не пустое
3. После добавления/изменения переменных перезапустите сервис

### Проблема: "Файл videos.json не найден"
**Решение:**
1. Убедитесь, что файл `app/videos.json` есть в репозитории
2. Проверьте, что файл закоммичен и отправлен в GitHub
3. Выполните новый деплой после добавления файла

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` - должен быть правильным
2. Проверьте логи на наличие ошибок
3. Убедитесь, что сервис запущен (статус Live)

---

## Альтернативный способ: One-off Command (если Shell недоступен)

Если Shell недоступен, можно использовать One-off Command:

1. В настройках сервиса найдите раздел **Manual Deploy**
2. В поле **Run Command** введите:
   ```
   python setup_db.py
   ```
3. Нажмите **Run Command**
4. Дождитесь выполнения и проверьте вывод

---

## Чек-лист готовности

Перед завершением убедитесь:

- [ ] PostgreSQL база данных создана на Render
- [ ] Internal Database URL скопирован
- [ ] Background Worker создан и настроен
- [ ] Все переменные окружения установлены:
  - [ ] `DATABASE_URL` (Internal URL!)
  - [ ] `TELEGRAM_BOT_TOKEN`
  - [ ] `GROQ_API_KEY`
- [ ] Скрипт `setup_db.py` выполнен успешно через Shell
- [ ] Сервис запущен (статус Live)
- [ ] Бот отвечает на команду `/start` в Telegram
- [ ] Бот отвечает на тестовый запрос

---

## Важные замечания

1. **Всегда используйте Internal Database URL** для подключения между сервисами Render
2. **External Database URL** используется только для подключения извне (например, с локальной машины)
3. После изменения переменных окружения нужно **перезапустить сервис**
4. Файл `app/videos.json` должен быть в репозитории для загрузки данных

---

## Готово! 🎉

Ваш бот развернут на Render и готов к работе. Теперь пользователи могут задавать вопросы на русском языке, и бот будет возвращать аналитику из базы данных.
